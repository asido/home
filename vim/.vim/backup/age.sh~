#!/bin/bash

echo -n "Type your age: "
read age

if [[ $age > 30 && $age < 60 ]]; then
	echo "wow, in $((60 - $age)) you are going to be 60!"
elif [[ $age < 30 ]]; then
	echo "You are too young to play this game"
else
	echo "You are too old to play this game"
fi
