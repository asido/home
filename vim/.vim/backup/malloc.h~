#ifndef MALLOC_5H9P6RCT
#define MALLOC_5H9P6RCT

void *malloc(size_t size);
void free(void *ptr);

struct alloc_info {
	size_t total_mem_free;
	int total_nodes_free;
	size_t *first_node;
	size_t *last_node;
};

static struct alloc_info get_alloc_info(void)
{
}

#endif /* end of include guard: MALLOC_5H9P6RCT */
