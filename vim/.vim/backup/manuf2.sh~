#!/bin/bash

if [ $# != 2 ]; then
	echo "Usage: $(basename $0) field file" 1>&2;
	exit 1;
fi

gawk -v "field=$1" < $2 '
		{count[$field]++}
		END {
			for(item in count)
				printf "%-20s%-20s", item, count[item];
		} ' | sort;
