#!/usr/bin/perl -w
# next = continue
# last = break

@array = ("one", "two", "three", "four", "five", "six", "seven");

for(@array) {
	if($_ eq "three") {
		next;
	}
	elsif($_ eq "five") {
		last;
	}
	print $_ , "\n";
}
