#!/usr/bin/perl -w

print "Enter the word 'five': ";
$entry = <>;
# chomp function removes the new line symbol from the end of the entry.
# Otherwise, the string would never match.
#chomp($entry);
if($entry eq "five") {
	print "Thank you for entering 'five'.\n";
}
print "End.\n";
