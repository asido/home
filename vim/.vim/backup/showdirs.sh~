#!/bin/bash

if [ $# != 1 ]; then
	echo "Usage: $(basename $0) directory"
	exit 1
fi

files=( $(ls -1 $1) )
directories=
index=0
for file in ${files[@]}; do
	if [ -d "$1/$file" ]; then
		directories[index]="$file"
		((index++))
	fi
done

for file in ${directories[@]}; do
	echo "$file"
done
