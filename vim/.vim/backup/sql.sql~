use music;

create table `users`
(
	`id` int not null auto_increment,
	`email` varchar(100),
	`password` varchar(40),
	`zip` varchar(30),
	`band_name` varchar(100),
	`paypal` varchar(100),
	`cid` varchar(40),
	`active` tinyint default 0,
	primary key (`id`)
);
