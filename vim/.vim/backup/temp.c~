#include <stdio.h>

float fahr2celc(float fahr);

main() {
	float fahr;
	int lower = 0;
	int upper = 300;
	int step = 20;

	for(fahr = lower; fahr <= upper; fahr += step)
		printf("%3.0ffahr =  %5.1fC\n", fahr, fahr2celc(fahr));
}

float fahr2celc(float fahr) {
	return (5 * (fahr - 32) / 9);
}
