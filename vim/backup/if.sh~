#!/bin/zsh

echo -n "1st word: "
read word1
echo -n "2nd word: "
read word2

if [ "$word1" = "$word2" ] # to put variable into "" is a good idea in case the user types words separated with spaces
	then
		echo "The words match"
fi
