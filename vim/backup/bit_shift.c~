#include <stdio.h>
#include <stdlib.h>

unsigned rightrot(unsigned x, int n);

int main() {
	int nr, n;
	printf("Digit: ");
	scanf("%d", &nr);
	printf("Shift value: ");
	scanf("%d", &n);
	printf("%d\n", rightrot(nr, n));

	return 0;
}

unsigned rightrot(unsigned x, int n) {
	return (x >> n);
}
